[project]
name = "qwen3omni"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "transformers",
]

[tool.uv.sources]
transformers = { git = "https://github.com/huggingface/transformers" }
