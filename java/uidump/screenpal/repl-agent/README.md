Steps

```sh
tail  -F /Users/wesdemos/Library/ScreenPal-v3/app-0.log -100

# compiles and reloads the repl agent using RELOADER agent (built in dir above this, manual commaneds in a build.sh script up there)
./run-repl-agent.fish
```
